This directory is specifically for the tally box designs that users can toggle between.

*Note*: All boxes use their own CSS styling except for a shared animations.css file that contains all the keyframe animations for the boxes